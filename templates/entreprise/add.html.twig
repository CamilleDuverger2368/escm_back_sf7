{% extends "base.html.twig" %}

{% block title %}Add Entreprise{% endblock %}

{% block body %}
<section id="form-add-entreprise" class="container">
    {{ form_start(form) }}
        <div class="info">
            <div class="label">
            {{ form_label(form.name) }}
            </div>
            {{ form_widget(form.name) }}
        </div>
        <div class="info">
            <div class="label">
            {{ form_label(form.cities) }}
            </div>
            {{ form_widget(form.cities) }}
        </div>
        <div class="submit-button">{{ form_row(form.enregistrer) }}</div>
    {{ form_end(form) }}
</section>
{% endblock %}